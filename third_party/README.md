# Third-party source

Run `bash scripts/setup_taxai.sh` to create `third_party/TaxAI` at the pinned
revision. That checkout is ignored by Git and excluded from release archives.

Do not vendor TaxAI until its redistribution terms have been confirmed.
